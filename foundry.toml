[profile.default]
src = "src"
out = "out"
libs = ["lib"]

solc_version = '0.8.25'
optimizer = true
optimizer_runs = 2000

[rpc_endpoints]
amoy = "${AMOY_RPC_URL}"

[etherscan]
amoy = { key = "${POLYGONSCAN_API_KEY}" }
